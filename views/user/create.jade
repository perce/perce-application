doctype html
html(lang="en")
  head
    title koa-render
    script.
      var foo = true;

      if (foo) {
         bar()
      }

      function bar() {
        console.log('everything works.');
      }

    style.
      body {
        font-family: Helvetica;
        text-align: center;
      }


      h1 {
        padding-top: 300px;
        font-weight: 200;
        color: #333;
        font-size: 6em;
      }
  body
    h1 koa on create ♥
    p You're new here, right? Wanne create an account?
    form(method="POST", action="/user")
      input(type="email", name="email")
      input(type="password", name="password")

      button Create it yeah!!!
